# GitHub Pages는 Settings에서 직접 설정하므로 이 파일은 필요하지 않습니다.
# 하지만 나중에 고급 기능을 위해 보관합니다.

name: Deploy to GitHub Pages (Disabled)

on:
  workflow_dispatch: # 수동 실행만 허용

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false # 비활성화
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
